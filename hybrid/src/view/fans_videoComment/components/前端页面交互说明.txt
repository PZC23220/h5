bridge.callHandler('getToken', {'targetType':'0','targetId':'0'}, function responseCallback(responseData) {
    self.getRanking(responseData.token);
})


<!-- h5调用客户端的方法 -->
<!-- bridge.callHandler(方法名, 参数, 回调函数) -->
<!-- 应是一个对象 -->

1.拿token（客户端将h5传过来的参数加上token后回调h5）
bridge.callHandler('getToken', {'targetType':'0','targetId':'0'}, function responseCallback(responseData) {})

2. 查看idol主页
bridge.callHandler('showIdolPage', {'idolId': val})

3.idol排行榜送礼 （context参数直接回调给h5）
bridge.callHandler('send_gift', {'context':'0','idol_id':val}, function responseCallback(responseData) {})

5.关闭当前h5页面
bridge.callHandler('close');

6.视频留言板（判断用户是否登录，登录之后才能留言）
<!-- responseData: {win_show: true/fasle} -->
bridge.callHandler('can_post', function(responseData){});

7.弹出toast
bridge.callHandler('makeToast', '服务器出错，请稍后重试');

8.发表评论成功后通知客户端有最新评论（res是评论成功后后台返回给前端的数据）
bridge.callHandler('did_post',{'post':res});

9.idol端留言板发布留言
bridge.callHandler('toPublish')

10.idol端留言板查看大图
bridge.callHandler('showImage',  {urls: [], selectedIndex: 0})









<!-- 客户端调用h5的方法 -->

1.idol排行榜刷新
bridge.registerHandler('ranking_refresh', function() {})

2.点击添加评论进入视频详情页
bridge.registerHandler('addComment', function() {})

11.idol端刷新留言板
bridge.registerHandler('comment_refresh', function() {})


